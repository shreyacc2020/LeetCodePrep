class Solution:
    def isPalindrome(self, x: int) -> bool:
        x_new = str(x)
        if x_new[0] == "-":
            return False
        x_new = x_new[::-1]
        y = int(x_new)
        if y == x:
            return True
        else:
            return False
